using namespace std;
#include <iostream>
#include <map>
#include <vector>

class a{
    private:
        int unint;
        string b;
    public:
        a(int a=0);
        ~a();
        void function(string b);
};

/* aller voir le code de la sae cpp */
